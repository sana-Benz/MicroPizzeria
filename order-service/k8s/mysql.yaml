apiVersion: v1 # PVC
kind: PersistentVolumeClaim # the disk survives even if mysql crashes and restarts
metadata:
  name: mysql-order-pvc # name of the persistant volume
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1 # DEPLOYMENT
kind: Deployment
metadata:
  name: mysql-order
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-order
  template:
    metadata:
      labels:
        app: mysql-order
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env: # read on first startup only
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-service-secret
                  key: mysql-root-password
            - name: MYSQL_DATABASE
              value: "orders_db"
            - name: MYSQL_USER
              value: "micropizza"
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-service-secret
                  key: mysql-password
          volumeMounts:
            - name: mysql-data
              mountPath: /var/lib/mysql
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "localhost"]
            initialDelaySeconds: 20
            periodSeconds: 10
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: mysql-order-pvc
---
apiVersion: v1 # SERVICE 
kind: Service
metadata:
  name: mysql-order
  namespace: default
spec:
  selector:
    app: mysql-order
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
