apiVersion: v1
kind: Service
metadata:
  name: order-service # DNS hostname ie any pod can reach this service with http://order-service:5002
spec:
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002
  type: ClusterIP