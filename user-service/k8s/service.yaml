apiVersion: v1
kind: Service
metadata:
  name: user-service        # DNS name inside the cluster
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 5001            # port other services use to reach this one
      targetPort: 5001      # port Flask is listening on inside the container
  type: ClusterIP           # internal only